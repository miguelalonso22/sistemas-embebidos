#ifndef BINTODEC_H
#define BINTODEC_H
#include <stdint.h>
#include <stdbool.h>
// Convierta un número binario en uno decimal:
int32_t bin2dec(int32_t binary, bool sign);
#include "BinToDec.c"
#endif
